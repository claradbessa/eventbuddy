; PHP per-directory settings for EventBuddy (PHP-FPM / FastCGI).
; Laragon uses PHP as FastCGI, so php_value in .htaccess has no effect —
; this .user.ini is the correct override mechanism (user_ini.cache_ttl = 300s).
upload_max_filesize = 20M
post_max_size       = 25M
